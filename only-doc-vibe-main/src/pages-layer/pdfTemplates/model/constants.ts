/** Templates shown per pagination page (catalog, category, subcategory). */
export const TEMPLATES_PER_PAGE = 9;

/** Public base path for the PDF templates catalog. */
export const TEMPLATES_BASE_PATH = "/pdf-templates";

/** Editor route. Opened with `?form=<template-slug>`; excluded from indexing. */
export const TEMPLATES_EDITOR_PATH = "/pdf-templates/editor";
