// Single switch for hypothesis-testing mock data. See docs/vibe/mock-data.md.
// Flip to true to make mocked api/ services return fake data instead of calling
// the real backend. Keep it false for anything meant to ship.
export const USE_MOCKS = false;
